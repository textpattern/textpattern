name    VARCHAR(255) NOT NULL DEFAULT '',
type    VARCHAR(28)  NOT NULL DEFAULT '',
Form    TEXT         NOT NULL,
skin    VARCHAR(255) NOT NULL DEFAULT 'default',
lastmod DATETIME         NULL DEFAULT NULL,

UNIQUE name_skin (name(200), skin(50))
